--- a/net/minecraft/block/BlockObserver.java
+++ b/net/minecraft/block/BlockObserver.java
@@ -1,6 +1,8 @@
 package net.minecraft.block;
 
+import carpet.logging.logHelpers.BUDLogHelper;
 import java.util.Random;
+import net.minecraft.init.Blocks;
 import net.minecraft.block.state.IBlockState;
 import net.minecraft.item.BlockItemUseContext;
 import net.minecraft.state.BooleanProperty;
@@ -56,6 +58,16 @@
 
     public IBlockState updatePostPlacement(IBlockState stateIn, EnumFacing facing, IBlockState facingState, IWorld worldIn, BlockPos currentPos, BlockPos facingPos)
     {
+        // TISCM BUD logger
+        if (stateIn.get(FACING) == facing && !worldIn.isRemote())
+        {
+            IBlockState iblockstate = worldIn.getBlockState(currentPos.down());
+            if (iblockstate.getBlock() == Blocks.DEAD_FIRE_CORAL_BLOCK)
+            {
+                BUDLogHelper.onBlockUpdated(currentPos, "PP", worldIn.getDimension().getType().getId());
+            }
+        }
+        // end TISCM BUD logger
         if (stateIn.get(FACING) == facing && !stateIn.get(POWERED))
         {
             this.startSignal(worldIn, currentPos);
@@ -63,7 +75,23 @@
 
         return super.updatePostPlacement(stateIn, facing, facingState, worldIn, currentPos, facingPos);
     }
+    
 
+    // TISCM BUD logger
+    public void neighborChanged(IBlockState state, World worldIn, BlockPos pos, Block blockIn, BlockPos fromPos)
+    {
+        if (!worldIn.isRemote())
+        {
+            IBlockState iblockstate = worldIn.getBlockState(pos.down());
+            if (iblockstate.getBlock() == Blocks.DEAD_FIRE_CORAL_BLOCK)
+            {
+                BUDLogHelper.onBlockUpdated(pos, "NC", worldIn.getDimension().getType().getId());
+            }
+        }
+        super.neighborChanged(state, worldIn, pos, blockIn, fromPos);
+    }
+    // end TISCM BUD logger
+
     private void startSignal(IWorld worldIn, BlockPos pos)
     {
         if (!worldIn.isRemote() && !worldIn.getPendingBlockTicks().isTickScheduled(pos, this))
